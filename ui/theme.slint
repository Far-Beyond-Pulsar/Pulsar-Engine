export global Theme {
    out property <brush> colors-primary: #4B8BFF;
    out property <brush> colors-background: #0B0B0F;
    out property <brush> colors-surface: #16161A;
    out property <brush> colors-hover: #272732;
    out property <brush> colors-text: #FFFFFF;
    out property <brush> colors-text-secondary: #9CA3AF;
    out property <brush> colors-border: #2D2D3A;

    out property <length> radius-small: 4px;
    out property <length> radius-medium: 6px;
    out property <length> radius-large: 8px;

    out property <length> text-size-small: 12px;
    out property <length> text-size-medium: 14px;
    out property <length> text-size-large: 16px;
}